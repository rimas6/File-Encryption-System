### File Encryption System
A C-based file encryption and decryption system developed in a Linux (Ubuntu) environment.

The project uses the XOR algorithm to convert file data into an encrypted format and restore it back to its original form. It demonstrates basic concepts of data security, file handling, and encryption logic.

This project was implemented using C programming language with standard libraries for file input/output operations.
